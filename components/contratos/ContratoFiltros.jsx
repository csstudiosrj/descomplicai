import { useState } from 'react';

/**
 * Filtros da listagem de contratos.
 * Busca por nome/fornecedor + filtro por status.
 */
export default function ContratoFiltros({
  busca,
  setBusca,
  filtroStatus,
  setFiltroStatus,
  total,
  rascunhos,
  enviados,
  assinados,
}) {
  // TODO: implementar barra de busca + pills de status + cards de resumo

  return (
    <div>
      {/* placeholder — implementar após decisão de layout */}
    </div>
  );
}